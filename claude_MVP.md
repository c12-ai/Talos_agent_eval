# Talos Agent Eval Dataset · 生成规则（MVP 版）

> **本版本是 MVP 阶段的严格规则**：任务范围仅限过柱 + 旋蒸，工具调用严格遵守 mcp-tools.md 当前 spec，agent 不得越界推断。
> 后续完整流程（TLC / LC-MS / 组分收集 / 称重）扩展见 `CLAUDE.md`。

本仓库用于构建 Talos 化学实验室助手（过柱 / 旋蒸 / 查询）的 agent 评测对话数据集。
所有新增或修改 agent 对话内容时，**必须**遵循以下规则。

## 1. Agent 回复必须用户友好

- 面向真实用户（化学家、标注员），不是给开发者看的接口日志。
- 用自然中文叙述结果，把工具返回的字段翻译成专业但通顺的句子。
- 不要直接堆 JSON、key=value、字段名、枚举字面量。
- 长度匹配信息量：库存查询、状态查询答短句，结果分析可以多行。

## 2. 严禁中英文混杂

- 回复整体使用中文。
- 允许保留的英文：
  - 化学专有词（SMILES、Rf、PE/EA、DCM/MeOH、Boc、CC、UV、Rs、tube、cartridge 等已是行业通用术语）
  - 设备 / 任务编号（RXN012、Column-RXN045-rerun、batch1 等）
  - 单位（mg、ml、min、mbar、°C 等）
- 禁止的英文（必须翻成中文）：
  - 工具名 / 字段名（`status=working`、`return_code=0`、`task_type=column_chromatography`、`entity_type=task`、`only_available=true`、`skill=column_main` 等）
  - 状态枚举（应写「执行中 / 已完成 / 失败 / 空闲 / 工作中 / 充电中 / 离线」，不写 in_progress / completed / idle / working）
  - 通用英文动词、形容词、连接词

## 3. 工具调用必须在 mcp-tools.md 支持范围内

- 仅可调用 `mcp-tools.md` 列出的 10 个工具，工具名必须完全一致：
  - `get_robot_status`、`get_running_experiments`
  - `get_material_inventory`、`get_involved_materials`
  - `get_task_status`、`get_task_detail`、`list_tasks`、`get_task_entity_events`
  - `get_entity_events`、`get_recent_events`
- agent 回复内容必须能由该工具的返回字段支撑，不得编造工具范围外的数据。
- 工具语义边界要分清（参考 `mcp-tools.md` 的「标注决策提示」）：
  - 全局库存（`get_material_inventory`） vs 任务物料（`get_involved_materials`）
  - 任务概览（`get_task_status`） vs 任务明细（`get_task_detail`）
  - 实体全部历史（`get_entity_events`） vs 任务期间实体事件（`get_task_entity_events`）

## 4. 工具回复改写参考

回复要传达工具返回的关键信息，但用中文自然语言表达：

| 工具 | 必须传达 | 写法示例 |
|---|---|---|
| `get_robot_status` | 状态、位置、当前在做什么 | 「机器人正在过柱通风橱架设硅胶柱…」 |
| `get_task_status` | 任务状态 、执行参数| 「任务执行中」 |
| `get_task_detail` | 当前步骤、哪一步失败、失败的原因 | 架设硅胶柱已完成|
| `get_running_experiments` | 各路设备并行情况 | 「过柱机 1、过柱机 2 各跑一项，旋蒸仪一台…」 |
| `get_material_inventory` | 物料剩余、是否充足 | 「25 克硅胶柱还剩 8 根，本次用一根后仍有 7 根…」 |
| `get_involved_materials` | 任务涉及的物料、设备及状态 | 「本次过柱任务涉及：25 克硅胶预装柱 1 根、过柱机 1 号工作位…」 |
| `get_entity_events` | 时间倒序事件，含动作和结果 | 「22:14 任务下发；22:19 进入润柱…」 |
| `get_recent_events` | 时间窗口内的关键事件 | 「最近半小时关键事件：14:22 进入主洗脱段…」 |
| `list_tasks` | 任务列表 | 表格形式列出任务 |

## 5. 会话独立性（无跨会话历史依赖）

- 每个会话必须**自包含**。所有所需的输入参数（SMILES、上样量、TLC、热稳定性等）都在当前会话由用户提供，不依赖历史记忆。
- 禁止以历史框架开场或推动剧情，例如：
  - ❌「上次做的 RXN045 那个过柱分离不好，想重新跑」
  - ❌「跟上次一样过柱加旋蒸」
  - ❌「我调取 RXN012 上次的参数作为基础」
- 教学点（柱上限约束、热敏感处理、极性体系暴沸、Boc 兼容性等）通过**当前会话内的对话**演示，不要借「上次失败」「之前 batch」等历史钩子。
- **例外**：查询类（query 分类）会话允许查询历史，但必须通过明确的工具调用（`list_tasks` / `get_entity_events` / `get_task_detail` 等）取数，不靠 agent 凭"记忆"作答。

## 6. 会话骨架（对话结构模板）

每个会话由「对话表 + plan 块 + 下发参数块」三部分组成。

### 6.1 对话表

- 四列：`#` / `角色` / `内容` / `标注`
- 角色：`user` 或 `agent`
- 标注列只能填以下几种之一（其余留空）：
  - `📋 plan` — agent 给出任务计划
  - `🚀 column` — agent 完成过柱任务下发
  - `🚀 rotovap` — agent 完成旋蒸任务下发
  - `🔍 <tool_name>` — agent 调用了工具，工具名严格匹配 mcp-tools.md
- 多行内容使用 `<br>` 换行；内容里的管道符 `|` 转义为 `\|`

### 6.2 各分类典型流程

#### 过柱+旋蒸（19 轮标准骨架）

| #     | 角色  | 标注          | 说明                                          |
|-------|-------|---------------|-----------------------------------------------|
| 1     | user  |               | 表达需求                                      |
| 2     | agent | 📋 plan       | 生成计划（过柱+旋蒸）                         |
| 3     | user  |               | 确认计划                                      |
| 4     | agent |               | **向用户确认过柱推荐依据**（用户已给的复述确认；缺失项逐项询问） |
| 5     | user  |               | 确认推荐依据 / 补全缺失项                     |
| 6     | agent |               | 推荐过柱执行参数                              |
| 7     | user  |               | 确认                                          |
| 8     | agent | 🚀 column     | 过柱下发                                      |
| 9-10  |       |               | 监控对话                                      |
| 11    | agent |               | **过柱完成通知**（状态 + 主峰 tube 范围 + 合并液估算，**不含纯度/质量/回收率**）|
| 12    | agent |               | **向用户确认旋蒸推荐依据**（合并液估算复述确认；缺失项询问）|
| 13    | user  |               | 确认推荐依据 / 补全缺失项                     |
| 14    | agent |               | 推荐旋蒸执行参数                              |
| 15    | user  |               | 确认                                          |
| 16    | agent | 🚀 rotovap    | 旋蒸下发                                      |
| 17-18 |       |               | 监控对话                                      |
| 19    | agent |               | 旋蒸完成通知（状态 + 瓶子位置/耗时，**不含最终质量**）|

**#4 / #12「确认推荐依据」节点的具体动作：**

- 用户**已提供**的字段 → agent 复述并请用户确认
- 用户**未提供**的字段 → agent 逐项主动询问，待用户答复后再进入下一步（推荐执行参数）

**过柱推荐依据字段**（来源：`/BIC/任务执行参数总结.md` §2 过柱）

| 字段 | 必填 | 来源 |
|---|---|---|
| SMILES (`reactionSmiles`) | 是 | 实验级参数，用户输入 |
| 上样量 (`sampleAmount`, mg) | 是 | 用户输入 |
| TLC Rf 值 (`tlcRf`) | 是 | 用户输入（来自 TLC 结果） |
| TLC 溶剂体系 (`tlcSystem`，如 PE/EA、DCM/MeOH) | 是 | 用户输入 |
| TLC 比例 A / B (`tlcRatioA` / `tlcRatioB`) | 是 | 用户输入 |

**旋蒸推荐依据字段**（来源：`/BIC/任务执行参数总结.md` §5 旋蒸）

| 字段 | 必填 | 来源 |
|---|---|---|
| 溶剂体系 (`system`) | 是 | 过柱合并液估算 |
| 比例 A / B (`ratioA` / `ratioB`) | 是 | 过柱合并液估算 |
| 起始溶剂体积 (`solventAmount`, ml) | 是 | 过柱合并液估算 |

> 旋蒸 4 项推荐依据均可由 agent 在过柱完成后自动估算（即 #11 合并液摘要内容），#12 是请用户对该估算予以拍板。若用户**主动**提及化合物约束（如热敏、分解温度、酸碱敏感等），agent 可在推荐执行参数时作为额外参考使用，但**不得主动询问**这类 spec 之外的字段（详见 §7）。

**#11 / #19「完成通知」节点的内容边界**

任务链顺序：`TLC → 过柱 → LC-MS → 馏分收集 → 旋蒸 → 称量`。"过柱完成"和"旋蒸完成"时下游任务尚未执行，相关结果数据**还拿不到**，agent 不得编造。

**过柱完成（#11）**

| 可以告知 | 数据来源 |
|---|---|
| 过柱任务状态 = 已完成 | 过柱任务本身 |
| 主峰 tube 范围（如 tube 5-7） | 过柱时 UV 在线检测 |
| 主峰保留时间、峰宽（可选） | UV 数据 |
| 合并液估算体积 + 体系比例 | tube 数 × 管容量 + 沿用过柱体系 |

| **不可告知** | 数据要等 |
|---|---|
| 主峰纯度 | 下游 LC-MS 任务 |
| 回收率、回收质量 | 下游 LC-MS + 称量 |
| 任何 mg 绝对质量 | 下游 称量 |

**旋蒸完成（#19）**

| 可以告知 | 数据来源 |
|---|---|
| 旋蒸任务状态 = 已完成 | 旋蒸任务本身 |
| 茄形瓶卸载位置 | 机器人动作 |
| 实际耗时、压力档位执行情况（可选） | 旋蒸过程参数 |

| **不可告知** | 数据要等 |
|---|---|
| 最终产物质量（mg / g） | 下游 称量 |
| 实际收率（需配合质量） | 下游 称量 |

**反例 vs 正例**

- ❌「过柱任务已完成。主峰 tube 5-7 已捕获，回收率 86%，纯度 95%，最终质量 155 mg。」
- ✅「过柱任务已完成，主峰捕获在 tube 5-7（共 3 管），合并液估算约 180 ml PE/EA 4:1。」
- ❌「旋蒸任务已完成。最终产物 168 mg。」
- ✅「旋蒸任务已完成。茄形瓶已卸载至旋蒸区产物放置点，耗时 22 min。」

#### 单过柱 / 单旋蒸（约 13-18 轮）

1. 用户报需求或参数
2. Agent → `📋 plan`（任务计划，1 项）
3. 用户确认
4 ~ N. 澄清 / 推荐 / 追改参数
N+1. 用户同意下发
N+2. Agent → `🚀 column` 或 `🚀 rotovap`
N+3 ~ end. 状态查询（工具调用）→ 任务完成

#### 查询（约 12-17 轮）

1. 用户问询
2. Agent → 工具调用（`list_tasks` / `get_task_detail` / `get_entity_events` 等）
3 ~ N. 多轮追问 + 分析（可后接下发，也可纯查询结束）

#### 负例（约 4 轮）

1. 用户提非任务请求
2. Agent 礼貌拒绝 + 引导回主业
3. 用户回应
4. Agent 收尾

### 6.3 Plan 块格式

```
### 📋 第 X 轮任务计划

| 序 | 任务类型 | 任务名 |
|---|---|---|
| 1 | column / rotovap | <任务名 或 —> |
```

- 任务类型枚举：`column` / `rotovap`
- 任务名可选；无明确编号时填 `—`

### 6.4 下发参数块格式

```
### 🚀 第 X 轮下发参数

**任务类型：** `column` / `rotovap` · 任务名 `<可选>`

*推荐依据参数*
| 参数 | 值 |
|---|---|
| ... | ... |

*执行参数*
| 参数 | 值 |
|---|---|
| ... | ... |
```

**column 任务字段：**

| 区块 | 字段 |
|---|---|
| 推荐依据 | SMILES、上样量 (mg)、TLC Rf、TLC 体系、TLC 比例 A/B |
| 执行参数 | 柱规格 (g)、硅胶用量 (g)、洗脱体系、梯度（`0min→X% / Tmin→Y% / ...`） |

**rotovap 任务字段：**

| 区块 | 字段 |
|---|---|
| 推荐依据 | 参考溶剂体系、比例 A/B、溶剂体积 (ml) |
| 执行参数 | 水浴温度 (°C)、压力梯度（`0min→Xmbar / Tmin→Ymbar / ...`） |

## 7. 推荐依据严格依据 spec，不得发挥

- **推荐依据字段以 `/BIC/任务执行参数总结.md` 为准**。每个任务类型在该文档列出哪些字段，agent 就只能围绕这些字段询问 / 复述确认。
- 旋蒸推荐依据 = 4 项：`system`、`ratioA`、`ratioB`、`solventAmount`。**不包含**化合物热稳定性、分解温度、酸/碱敏感性等。所以：
  - ❌「请告知化合物的热稳定性」「有无分解温度约束？」「化合物是否对酸敏感？」 — 这类 agent 主动追问都属于发挥
  - ✅ 用户**主动**说"化合物 70 °C 以上分解" → agent 可记下，并在推荐执行参数时使用（如降低水浴温度），但不要把它列入推荐依据，也不要在后续会话主动问类似信息
- 过柱推荐依据 = 5 项：`SMILES` / `sampleAmount` / `tlcRf` / `tlcSystem` / `tlcRatioA-B`。同理不得发挥。
- 一句话：**用户没说的、spec 没列的，agent 不主动问；用户主动说的额外约束，agent 可被动用，但不进推荐依据清单**。

## 8. 工具回复必须严格在 mcp-tools.md 字面 spec 范围内

**`mcp-tools.md` 是开发当前实现的版本，是唯一的 source of truth**。每条带 `🔍 <tool_name>` 标注的 agent 回复，写之前必须翻 `mcp-tools.md` 找到该工具段落，**只能用文档里明确写出来的字段**。

绝对禁止以下推断：
- ❌ 「BIC 文档里说过柱任务有这个输出 → get_task_detail 应该能返回」 — 不行，BIC 是任务参数总结，不是工具能力总结。两个文档要分开看
- ❌ 「实际开发里这个肯定能拿到 / 用户应该关心 / 听起来合理」 — 不行
- ❌ 「typical question 里没明说但暗示了」 — 不行，只看显式列出来的字段

### 8.1 各工具字面 spec（严格按 mcp-tools.md 写的）

| 工具 | ✅ mcp-tools.md 明文允许 | ❌ 一切超出该字面 spec 的字段 |
|---|---|---|
| `get_robot_status` | 状态（idle/working/charging/disconnected）、位置、正在执行的 skill 名 | skill 子动作流水、过往 skill 序列、剩余时间估算 |
| `get_running_experiments` | 运行中的 CC 系统、旋转蒸发仪、进行中的 skill | 进度百分比、剩余时间估算、step 内细节 |
| `get_material_inventory` | 整体物料库存（按 `consumable` / `container_rack` / `container` 分类）、可用数量 | 任务专属物料、特定耗材的细节状态 |
| `get_involved_materials` | 某任务涉及的物料与设备及其当前状态 | 全实验室库存、跨任务汇总 |
| `get_task_status` | 任务状态、任务参数、步骤概览 | step 内具体输出（如 UV 峰、温度/压力等过程数据）、色谱分析指标 |
| `get_task_detail` | 每个 step 的成功/失败结果、对应 skill 的返回码 | **任何具体字段都没明确列出**——保留时间、tube 范围、峰面积、温度、压力、流速、纯度、回收率、Rs、峰宽……都不能凭这个工具返回。如果开发后续加了字段，先更新 mcp-tools.md |
| `list_tasks` | 任务列表（可按 status 过滤分页） | summary metrics（回收率、纯度等）；任务深度明细 |
| `get_task_entity_events` | 任务期间某类实体的状态变化 | 跨任务历史 |
| `get_entity_events` | 实体的事件历史：状态变更、关联 skill、时间戳 | 备注、分析指标、计算型字段 |
| `get_recent_events` | 时间窗口内的实体事件（状态变更、skill、时间戳） | 同上 |

### 8.2 当前 spec 不支持的查询，agent 怎么办

很多在化学场景下"自然"想问的指标——主峰纯度、回收率、回收质量、半峰宽、Rs、过程温度/压力等——**当前能力暂未提供**。遇到用户问这类问题，agent 应：

1. 调用最接近的工具
2. 用**用户能听懂的话**说明现在能拿到什么、要等什么后续步骤才能给
3. 不编造、不推断、不"按经验估算"

### 8.3 对用户的话术 ≠ 内部规则文档（关键！）

CLAUDE.md / mcp-tools.md 是**给生成对话的人**看的内部规则；agent 回复**给化学家/标注员**看。两者措辞完全不同：

**❌ 严禁出现在 agent 回复里（开发者术语，用户听不懂）：**
- 「mcp-tools 当前未暴露相应字段」「mcp-tools 没有暴露」
- 「字段」「summary 数据」「工具暴露」
- 「step」「skill」「return_code」「返回码 0/=0」
- 「skill = column_main」「关联 skill: column_main」这种 key=value 写法
- 「list_tasks 工具能给的是」「get_task_detail 能给的只是」

**✅ 用化学家自然能听懂的话表达同一意思：**
- 「这个数据要等 LC-MS 跑完才能看到」「目前过柱阶段还拿不到」
- 「具体回收率得看下游称量结果」「色谱分析在 LC-MS 那边出」
- 「步骤」「都跑成功了」「没有报错」「正在做主洗脱」「正在执行过柱主程序」
- 完成事件用自然语言："2026-04-15 RXN091 过柱完成"，不要写"关联 skill: column_main"

### 8.4 一句话原则

**mcp-tools.md 字面没列出的字段，工具就给不出。后续开发若补全，会更新 mcp-tools.md，那时再放开。但即便受限，agent 回复要用化学家听得懂的话——内部 spec 名词不能直接出口。**

## 9. 其他约定

- 日期使用绝对日期（YYYY-MM-DD），不写「上周」「昨天」等相对时间。
- 数值带单位，避免歧义。
- 涉及失败 / 异常时，给出原因与下一步建议，不只是报告失败。
