# Talos Agent Eval Dataset · 生成规则

本仓库用于构建 Talos 化学实验室助手（过柱 / 旋蒸 / 查询）的 agent 评测对话数据集。
所有新增或修改 agent 对话内容时，**必须**遵循以下规则。

## 1. Agent 回复必须用户友好

- 面向真实用户（化学家、标注员），不是给开发者看的接口日志。
- 用自然中文叙述结果，把工具返回的字段翻译成专业但通顺的句子。
- 不要直接堆 JSON、key=value、字段名、枚举字面量。
- 长度匹配信息量：库存查询、状态查询答短句，结果分析可以多行。

## 2. 严禁中英文混杂

- 回复整体使用中文。
- 允许保留的英文：
  - 化学专有词（SMILES、Rf、PE/EA、DCM/MeOH、Boc、CC、UV、Rs、tube、cartridge 等已是行业通用术语）
  - 设备 / 任务编号（RXN012、Column-RXN045-rerun、batch1 等）
  - 单位（mg、ml、min、mbar、°C 等）
- 禁止的英文（必须翻成中文）：
  - 工具名 / 字段名（`status=working`、`return_code=0`、`task_type=column_chromatography`、`entity_type=task`、`only_available=true`、`skill=column_main` 等）
  - 状态枚举（应写「执行中 / 已完成 / 失败 / 空闲 / 工作中 / 充电中 / 离线」，不写 in_progress / completed / idle / working）
  - 通用英文动词、形容词、连接词

## 3. 工具调用以 mcp-tools.md 为参考（不死守）

`mcp-tools.md` 列出的是**开发当前已实现的工具**，作为 agent 工具调用名称的参考——`🔍 <tool_name>` 标注里的工具名要从 mcp-tools.md 列表里选。

但 agent 回复**不必死守现有 spec**：如果场景需要给出当前 spec 没暴露的字段（如色谱质量指标、过程实时参数、化合物纯度、回收率、半峰宽、Rs 等），agent 可以按"未来即将实现的能力"自然给出。我们生成的对话是为了覆盖产品**完整能力图谱**，mcp-tools.md 会随后续开发不断扩展。

工具语义边界仍然要分清（这是为了选对工具，不是限制返回内容）：
- 全局库存（`get_material_inventory`） vs 任务物料（`get_involved_materials`）
- 任务概览（`get_task_status`） vs 任务明细（`get_task_detail`）
- 实体全部历史（`get_entity_events`） vs 任务期间实体事件（`get_task_entity_events`）

## 4. 工具回复改写参考

回复要传达工具返回的关键信息，但用中文自然语言表达：

| 工具 | 必须传达 | 写法示例 |
|---|---|---|
| `get_robot_status` | 状态、位置、当前在做什么 | 「机器人正在过柱通风橱架设硅胶柱…」 |
| `get_task_status` | 任务状态 、执行参数| 「任务执行中」 |
| `get_task_detail` | 当前步骤、哪一步失败、失败的原因 | 架设硅胶柱已完成|
| `get_running_experiments` | 各路设备并行情况 | 「过柱机 1、过柱机 2 各跑一项，旋蒸仪一台…」 |
| `get_material_inventory` | 物料剩余、是否充足 | 「25 克硅胶柱还剩 8 根，本次用一根后仍有 7 根…」 |
| `get_involved_materials` | 任务涉及的物料、设备及状态 | 「本次过柱任务涉及：25 克硅胶预装柱 1 根、过柱机 1 号工作位…」 |
| `get_entity_events` | 时间倒序事件，含动作和结果 | 「22:14 任务下发；22:19 进入润柱…」 |
| `get_recent_events` | 时间窗口内的关键事件 | 「最近半小时关键事件：14:22 进入主洗脱段…」 |
| `list_tasks` | 任务列表 | 表格形式列出任务 |

## 5. 会话独立性（无跨会话历史依赖）

- 每个会话必须**自包含**。所有所需的输入参数（SMILES、上样量、TLC、热稳定性等）都在当前会话由用户提供，不依赖历史记忆。
- 禁止以历史框架开场或推动剧情，例如：
  - ❌「上次做的 RXN045 那个过柱分离不好，想重新跑」
  - ❌「跟上次一样过柱加旋蒸」
  - ❌「我调取 RXN012 上次的参数作为基础」
- 教学点（柱上限约束、热敏感处理、极性体系暴沸、Boc 兼容性等）通过**当前会话内的对话**演示，不要借「上次失败」「之前 batch」等历史钩子。
- **例外**：查询类（query 分类）会话允许查询历史，但必须通过明确的工具调用（`list_tasks` / `get_entity_events` / `get_task_detail` 等）取数，不靠 agent 凭"记忆"作答。

## 6. 目标用户角色（Personas）

数据集里 user 的发言要符合实验室真实场景下的用户视角。生成 conv 时**重点覆盖前两类角色**（化学研究员、小组长，约占 90%）；研发总监和实验室运营负责人作为补充。

### 6.1 实验室化学研究员（核心，~70% conv）

**画像：**
- 一线动手做实验的化学家
- 关注自己手头的具体反应、单个化合物的纯化
- 有化学专业知识，对参数有自己的理解和偏好

**典型问题：**
- 任务下发：「帮我过柱+旋蒸，化合物 SMILES X、上样 200 mg、TLC Rf 0.4 PE:EA 3:1」
- 参数讨论：「梯度起点能不能给低一点？」「柱规格用 12 g 还是 25 g？」「水浴温度 35°C 能蒸干吗？」
- 化学约束主动告知：「这化合物对空气敏感」「Boc 60°C 以上分解」「化合物 70 度会分解」
- 进度追踪：「机器人在干嘛？」「跑到哪了？」「还剩多久？」
- 失败排查：「为什么主峰拖尾？」「Boc 脱了没？」
- 结果评估：「回收率怎么样？」「主次峰分得开吗？」
- 个人库存查询：「25 g 柱我们还够吗？」「这次任务用什么物料？」

### 6.2 研究员小组长（核心，~20% conv）

**画像：**
- 带 3-5 名研究员的中层
- 不一定亲自动手，但要全局视角看团队进度、资源分配、机器人/物料调度
- 关注异常、效率、排队

**典型问题：**
- 团队进度：「组里今天的过柱都跑完了吗？」「这几个组员的任务都到哪一步了？」
- 资源调度：「实验室现在有几台机器在跑？」「过柱机什么时候空出来？」「今晚有没有过夜任务冲突？」
- 物料管理：「硅胶柱本周够用吗？」「下周要补什么？」
- 失败/异常审查：「这周组里有几个失败任务？」「设备最近有报警吗？」「过柱机 1 号传感器异常以前出现过吗？」
- 跨任务对比：「这几个嘧啶化合物的过柱回收率怎么样？」「同类化合物历史最佳梯度？」
- 排队规划：「我组里再下发 3 个过柱任务，预计什么时候能跑完？」
- 跨人员任务跟踪：「组员 A 那个 RXN108 现在怎么样？」

### 6.3 研发总监（补充，~5% conv）

**画像：**
- 项目级 / 月度级视角
- 不关心单条任务细节，关心趋势、产出、关键化合物推进

**典型问题：**
- 项目产出：「这个项目本月纯化了多少化合物？」
- 失败趋势：「最近一个月失败率最高的化合物结构类是哪些？」
- 效率对比：「实验室本月吞吐量比上月怎么样？」
- 关键化合物追踪：「先导化合物 X 系列的纯化进度？」「SAR 系列的关键中间体都拿到了吗？」

### 6.4 实验室运营负责人（补充，~5% conv）

**画像：**
- 关注物料采购、设备维护、成本、排班
- 不做化学决策，做资源 / 运营决策

**典型问题：**
- 耗材消耗：「本月各规格硅胶柱消耗量？」「PE/EA 还能撑几天？」
- 设备维护：「过柱机近一个月报警次数？」「该不该排保养？」「机器人最近充电频率正常吗？」
- 成本核算：「本月溶剂、耗材总消耗？」
- 调度规划：「下周排了多少任务？库存够吗？」「设备故障率高的位置？」

### 6.5 角色串味的反例

设计 conv 时先想清楚 user 是哪类角色，用该角色**自然会问的语气和深度**写问题。不要串味：

- ❌ 让小组长问「梯度起点能不能给低一点」（太具体，是研究员视角）
- ❌ 让一线研究员问「本月化合物纯化吞吐量」（太宏观，是总监视角）
- ❌ 让总监问「我手上这 200 mg 怎么过」（总监不会自己跑反应）
- ❌ 让运营负责人问「Boc 化合物的水浴温度怎么定」（不做化学决策）
- ✅ 研究员：聚焦单个化合物 / 单条任务的具体技术细节
- ✅ 小组长：聚焦团队 / 设备 / 物料的调度与异常监控
- ✅ 总监：聚焦项目趋势、效率、关键化合物
- ✅ 运营负责人：聚焦资源、维护、成本

## 7. 任务类型与完整流程

### 7.0 实验任务链

完整的纯化实验按以下顺序执行：

```
TLC（薄层色谱） → 过柱（Column） → 分析（LC-MS） → 组分收集（Fraction） → 旋蒸（Rotovap） → 称重（Weighing）
```

任务类型枚举（用于 plan / 🚀 标注 / 下发块）：

| 任务类型 | 中文名 | 用途 |
|---|---|---|
| `tlc` | TLC 薄层色谱 | 前置点板，确定 Rf 值与展开剂体系，为过柱提供依据 |
| `column` | 过柱（柱层析） | 主纯化操作，按梯度洗脱分离化合物 |
| `lcms` | 分析（LC-MS） | 过柱后对 tube 馏分做色谱-质谱分析，给出纯度、保留时间、Rs 等指标 |
| `fraction` | 组分收集 | 按 LC-MS 结果把 tube 分配到收集瓶或废液瓶 |
| `rotovap` | 旋蒸 | 蒸去溶剂得到固体或浓缩液 |
| `weighing` | 称重 | 称量最终产物质量，给出回收率 |

一次会话可以下发**任意子集或完整流程**，常见组合：

- 单 column / 单 rotovap：单一操作
- column + rotovap：常规快速纯化（不分析、不称重）
- 完整流程 tlc → column → lcms → fraction → rotovap → weighing：精确纯化
- query：纯查询，不下发

### 7.1 跨任务的数据流

各任务之间的数据流向（决定下游任务的"推荐依据"）：

| 上游任务 | 输出 | 下游任务的推荐依据 |
|---|---|---|
| TLC | 实测 Rf、确认体系/比例 | 过柱（沿用 Rf 与体系） |
| 过柱 | 馏分信息（peakId / tube 范围 / retentionTime / peakArea / status） | LC-MS（按 tube 选样分析） |
| LC-MS | 各 tube 纯度、Rs、峰形 | 组分收集（按纯度阈值分配 tube） |
| 组分收集 | 合并液体积、体系比例 | 旋蒸（设定水浴温度与压力梯度） |
| 旋蒸 | 蒸干后的瓶子 | 称重（最终质量） |
| 称重 | 质量、回收率 | — |

## 8. 会话骨架（对话结构模板）

每个会话由「对话表 + plan 块 + 下发参数块」三部分组成。

### 8.1 对话表

- 四列：`#` / `角色` / `内容` / `标注`
- 角色：`user` 或 `agent`
- 标注列只能填以下几种之一（其余留空）：
  - `📋 plan` — agent 给出任务计划
  - `🚀 tlc` — TLC 任务下发
  - `🚀 column` — 过柱任务下发
  - `🚀 lcms` — LC-MS 分析任务下发
  - `🚀 fraction` — 组分收集任务下发
  - `🚀 rotovap` — 旋蒸任务下发
  - `🚀 weighing` — 称重任务下发
  - `🔍 <tool_name>` — agent 调用了工具，工具名取自 mcp-tools.md
- 多行内容使用 `<br>` 换行；内容里的管道符 `|` 转义为 `\|`

### 8.2 通用任务块（每个任务的小骨架）

无论是 tlc / column / lcms / fraction / rotovap / weighing，每个任务在会话里遵循同样的小骨架：

| 步骤 | 角色  | 标注                | 说明                                                                                       |
|------|-------|---------------------|--------------------------------------------------------------------------------------------|
| a    | agent |                     | **向用户确认本任务推荐依据**（用户已给的复述确认；缺失项逐项询问）                          |
| b    | user  |                     | 确认推荐依据 / 补全缺失项                                                                  |
| c    | agent |                     | 基于推荐依据**推荐本任务执行参数**                                                         |
| d    | user  |                     | 确认                                                                                       |
| e    | agent | 🚀 \<task_type\>    | 任务下发                                                                                   |
| f    |       |                     | 监控对话（用户问进度 / agent 用工具回，可有可无）                                          |
| g    | agent |                     | **任务完成通知**（含可拿到的状态/输出；下游任务的推荐依据通常就在这里给出）                |

整个会话 = 计划 + 多个任务块串联：

| #    | 角色  | 标注       | 说明                                          |
|------|-------|------------|-----------------------------------------------|
| 1    | user  |            | 表达需求                                      |
| 2    | agent | 📋 plan    | 生成计划（一次性列出所有任务）                |
| 3    | user  |            | 确认计划                                      |
| 4+   | …     |            | 任务 1 的小骨架 a → g                          |
| …    |       |            | 任务 2 的小骨架 a → g                          |
| …    |       |            | …                                            |
| 末   | agent |            | 末尾任务完成通知                              |

### 8.3 常见组合骨架

**单任务**（如单 column / 单 rotovap）：约 13-18 轮
- 计划（plan）+ 1 个任务块

**双任务（column + rotovap）**：约 19-25 轮
- 计划（含两个任务）+ 2 个任务块

**完整流程（tlc → column → lcms → fraction → rotovap → weighing）**：约 35-50 轮
- 计划（含 6 个任务）+ 6 个任务块；任务间通过"完成通知 → 下游推荐依据"自然衔接

**查询类**：约 12-17 轮
- 用户问询 → agent 工具调用回应 → 多轮追问；可后接下发，也可纯查询结束

**负例**：约 4 轮
- 用户提非任务请求 → agent 礼貌拒绝 + 引导回主业 → 收尾

### 8.4 各任务的推荐依据 + 执行参数字段

来源：`/BIC/任务执行参数总结.md`。每个任务块步骤 a 要确认的就是「推荐依据」；步骤 c 给的是「执行参数」。

#### TLC（薄层色谱）

| 区块 | 字段 | 来源 |
|---|---|---|
| 推荐依据 | 目标 Rf 值 (`targetRf`) | 用户输入 |
| 推荐依据 | Rf 容差下限/上限 (`rfMinError` / `rfMaxError`) | 用户输入（默认 0.05 / 0.10） |
| 执行参数 | 洗脱体系 (`system`，PE/EA、DCM/MeOH) | agent 推荐 |
| 执行参数 | 比例 A / B (`ratioA` / `ratioB`) | agent 推荐 |
| 执行参数 | 预测 Rf (`predictedRf`) | 接口自动计算（SMILES + 体系 + 比例） |

#### 过柱（Column）

| 区块 | 字段 | 来源 |
|---|---|---|
| 推荐依据 | SMILES (`reactionSmiles`) | 实验级参数，用户输入 |
| 推荐依据 | 上样量 (`sampleAmount`, mg) | 用户输入 |
| 推荐依据 | TLC Rf (`tlcRf`) | TLC 任务结果或用户输入 |
| 推荐依据 | TLC 体系 + 比例 A/B (`tlcSystem` / `tlcRatioA` / `tlcRatioB`) | TLC 任务结果或用户输入 |
| 执行参数 | 柱规格 (`columnSpec`，4/12/25/40/120 g) | agent 推荐 |
| 执行参数 | 硅胶用量 (`silica`, g) | agent 推荐 |
| 执行参数 | 洗脱体系 (`eluent`，PE/EA、DCM/MeOH、PE/Et2O) | agent 推荐 |
| 执行参数 | 梯度 (`gradient`，JSON 数组：`[{time, ratio}, …]`) | agent 推荐 |

#### LC-MS（分析）

| 区块 | 字段 | 来源 |
|---|---|---|
| 推荐依据 | 过柱馏分信息：peakId、tubeStart、tubeEnd、retentionTime、peakArea、status | 过柱任务结果 |
| 执行参数 | 水相条件 (`aqueousPhase`，Neutral/Acidic/Basic) | agent 推荐 |
| 执行参数 | 有机相 (`organicPhase`，ACN/Methanol) | agent 推荐 |
| 执行参数 | 试管架 (`rackName`，Rack-1/Rack-2，可选默认 Rack-1) | agent 推荐 |
| 执行参数 | 选中试管 (`selectedTubes`，JSON：`{rackName: "A1-A5"}`) | 由过柱馏分推荐 |
| 执行参数 | TLC 确认开关 (`tlcConfirmEnabled`) + 相关字段 | 可选，默认关 |

#### 组分收集（Fraction）

| 区块 | 字段 | 来源 |
|---|---|---|
| 推荐依据 | 各 tube 的 LC-MS 分析结果（纯度、保留时间） | LC-MS 任务结果 |
| 执行参数 | 容器分配 (`containers`，JSON 数组：`[{id, type, name, tubes:[]}]`，type 为 flask 或 waste） | agent 按纯度阈值推荐 |

#### 旋蒸（Rotovap）

| 区块 | 字段 | 来源 |
|---|---|---|
| 推荐依据 | 溶剂体系 (`system`) | 组分收集合并液 |
| 推荐依据 | 比例 A / B (`ratioA` / `ratioB`) | 沿用过柱体系 |
| 推荐依据 | 起始溶剂体积 (`solventAmount`, ml) | 合并液估算 |
| 执行参数 | 水浴温度 (`temp`, °C) | agent 推荐 |
| 执行参数 | 压力梯度 (`pressureGradient`，JSON：`[{time, pressure}, …]`，单位 mbar) | agent 推荐 |

> 化合物的热敏 / 分解温度 / 酸碱敏感等约束**不在推荐依据 spec 内**，agent 不主动问；用户主动提及时 agent 可作为额外参考用于推荐执行参数（详见 §9）。

#### 称重（Weighing）

| 区块 | 字段 | 来源 |
|---|---|---|
| 推荐依据 | 目标质量 (`targetWeight`) | 实验级参数，用户输入（可选） |
| 执行参数 | 称重目标 (`weighingTarget`, g) | agent 推荐 |
| 执行参数 | 容差 (`weighingTolerance`, %) | agent 推荐 |

### 8.5 任务完成通知的内容边界

**总原则**：每个任务完成时，agent 报本任务的输出 + 自然衔接下游任务的推荐依据。

**任务链时序**：`TLC → 过柱 → LC-MS → 组分收集 → 旋蒸 → 称重`。

哪个任务跑了，对应的数据就有；没跑的任务对应数据就没有，**agent 不能编造**。常见情况：

| 会话只跑了 | 完成时能给到 | 给不到（因为下游没跑） |
|---|---|---|
| TLC | 实测 Rf、最终体系/比例 | — |
| 过柱（无 LC-MS） | 任务状态、主峰 tube 范围、合并液估算 | 主峰纯度、Rs、半峰宽（要 LC-MS） |
| 过柱 + LC-MS | 过柱：tube 范围 + 合并液估算；LC-MS：各 tube 纯度、Rs、峰形 | 回收率、最终质量（要称重） |
| 过柱 + LC-MS + 组分收集 + 旋蒸（无称重） | 上面全部 + 蒸干瓶子位置 + 耗时 | 最终质量、回收率（要称重） |
| 完整流程到称重 | 所有上述 + 最终质量 + 回收率 | — |

**反例 vs 正例**

- ❌（只跑过柱）"过柱完成。回收率 86%，纯度 95%，最终质量 155 mg。" — 没跑 LC-MS / 称重，这些字段编造
- ✅（只跑过柱）"过柱完成。主峰捕获在 tube 5-7，合并液估算约 180 ml PE/EA 4:1。"
- ✅（跑了过柱+LC-MS）"LC-MS 完成。tube 5-7 主峰纯度 95%、Rs 2.2，tube 6 边缘有微量副产物。"
- ✅（跑了完整流程）"称重完成。最终质量 155 mg，回收率 86%。"

### 8.6 Plan 块格式

```
### 📋 第 X 轮任务计划

| 序 | 任务类型 | 任务名 |
|---|---|---|
| 1 | tlc / column / lcms / fraction / rotovap / weighing | <任务名 或 —> |
```

- 任务类型枚举：`tlc` / `column` / `lcms` / `fraction` / `rotovap` / `weighing`
- 任务名可选；无明确编号时填 `—`

### 8.7 下发参数块格式

```
### 🚀 第 X 轮下发参数

**任务类型：** `<task_type>` · 任务名 `<可选>`

*推荐依据参数*
| 参数 | 值 |
|---|---|
| ... | ... |

*执行参数*
| 参数 | 值 |
|---|---|
| ... | ... |
```

每个 task_type 的具体字段见 §8.4。

## 9. 推荐依据严格依据 spec（化合物约束不得主动发挥）

每个任务类型的推荐依据字段已在 §8.4 详尽列出，agent 围绕这些字段询问 / 复述确认即可。

特别注意：**化合物的热敏性 / 分解温度 / 酸碱敏感等约束不在任何任务的推荐依据 spec 内**：

- ❌ agent 主动追问「化合物的热稳定性？」「有无分解温度约束？」「是否对酸敏感？」 — 这是发挥
- ✅ 用户**主动**说"化合物 70 °C 以上分解"——agent 可记下，并在推荐执行参数时使用（如降低旋蒸水浴温度），但不要列入推荐依据清单，也不要在后续主动问类似信息

**一句话原则**：用户没说的、spec 没列的，agent 不主动问；用户主动说的额外约束，agent 可被动用，但不进推荐依据清单。

## 10. 对用户的话术 ≠ 内部规则文档

CLAUDE.md / mcp-tools.md 是**给生成对话的人**看的内部规则；agent 回复**给化学家/标注员**看。两者措辞完全不同。

**❌ 严禁出现在 agent 回复里（开发者术语，用户听不懂）：**
- 「mcp-tools 当前未暴露相应字段」「mcp-tools 没有暴露」
- 「字段」「summary 数据」「工具暴露」
- 「step」「skill」「return_code」「返回码 0/=0」
- 「skill = column_main」「关联 skill: column_main」这种 key=value 写法
- 「list_tasks 工具能给的是」「get_task_detail 能给的只是」

**✅ 用化学家自然能听懂的话表达同一意思：**
- 「这个数据要等 LC-MS 跑完才能看到」「目前过柱阶段还拿不到」
- 「具体回收率得看称重结果」
- 「步骤」「都跑成功了」「没有报错」「正在做主洗脱」「正在跑过柱主程序」
- 完成事件用自然语言："2026-04-15 RXN091 过柱完成"，不要写"关联 skill: column_main"

## 11. 其他约定

- 日期使用绝对日期（YYYY-MM-DD），不写「上周」「昨天」等相对时间。
- 数值带单位，避免歧义。
- 涉及失败 / 异常时，给出原因与下一步建议，不只是报告失败。
